# Implementation Notes: rmcp 3.1 Fleet Cutover

Running record for `docs/design/2026-08-03-rmcp-3.1-fleet-cutover.md`. Append-only:
a later decision that overrides an earlier one gets a NEW entry that supersedes it,
never a rewrite.

## Phase 0: prove a real host drives a 3.1 server

### Design decisions
- Probe built at `~/.cache/rmcp-spike`, NOT under `/tmp`. The doc warns that three
  cargo target dirs filled a 16G tmpfs; measured `/tmp` on `desk.lan` at the time of
  the run: `tmpfs 16G, 12G used, 4.5G available`. So the warning was live, not
  historical, and a `/tmp` build would have hit it.
- Drove the spike from a real Claude Code client via
  `claude -p --mcp-config <scratch.json> --strict-mcp-config`, with the server named
  `clydespike` so it could not collide with the live `clyde` entry.
- Exercised `clyde mcp register` / `status` against a scratch `CLAUDE_CONFIG_DIR`
  (`~/.cache/rmcp-spike/claude-config`), which the `--target user` help documents as
  honored. This runs the real `mcp-io` register code path, just not against
  `~/.claude.json`.

### Deviations
- **The doc's "register, restart Claude Code, call one tool, then re-register the
  INSTALLED clyde afterwards" sequence was NOT followed as written.** Two reasons,
  and the proof obtained is the same or better:
  - Restarting Claude Code would have ended the session executing the plan. A fresh
    `claude -p` subprocess is a real Claude Code client with a real MCP client, so it
    proves the same environmental assumption without that.
  - `--mcp-config` + `--strict-mcp-config` and a scratch `CLAUDE_CONFIG_DIR` mean
    Scott's live `~/.claude.json` was never written at all. Verified before and after:
    the live `mcpServers.clyde` entry is still `{"command":"clyde","args":["mcp","serve"]}`,
    the bare-name form, NOT rewritten to an absolute scratch path.
  - Consequence: the doc's re-register cleanup step became unnecessary rather than
    skipped. The residue it existed to remove was never created. Note that a literal
    `register --target user` WOULD have rewritten that entry to
    `current_exe()`, i.e. the throwaway binary, exactly as the doc warned.

### Tradeoffs
- Scratch-config proof vs. registering into live config: the literal reading exercises
  the exact bytes a real host reads at `~/.claude.json`, which is marginally closer to
  production. Rejected because it mutates live developer config mid-plan for no extra
  signal, and the failure mode if the plan aborted between register and re-register is
  a live MCP server pointing at a deleted scratch binary.
- `clyde` as the carrier (doc's choice, kept): `mcp-io` ships no binary, and clyde
  needs no Okta round-trip to start.

### Open questions
- None.

### Evidence
- Spike built clean against rmcp 3.1.0, zero source changes: only
  `mcp-io/Cargo.toml` (rmcp 2.2 -> 3.1) and `clyde/clyde/Cargo.toml` (mcp-io -> path
  override) plus `clyde/sessions/Cargo.toml` (rmcp 2.1.0 -> 3.1) were edited. Probe
  lock resolved exactly one rmcp, at `3.1.0`.
- Criterion 1 (tools listed, one `tools/call` returns): MET. The client saw
  `session_efficiency`, `session_grep`, `session_open`, `session_read`, `sessions_ls`,
  `sessions_search`, and `sessions_ls` with `limit: 2` returned 2 real session records.
- Criterion 2 (`clyde mcp status` names the server `clyde`, not `rmcp`): MET, asserted
  on OUTPUT not exit code, per the doc's warning that `status` exits 0 even on a name
  mismatch. Observed `clyde: user -> not registered` before and
  `clyde: user -> registered` after, with `register` printing
  `clyde: registered 'clyde' (user)`.

## Phase 1: mcp-io-rs to rmcp 3.1, tagged

### Design decisions
- Version moved via `cargo add rmcp@3.1 ...` for both the dep and the dev-dep, not by
  hand-editing `Cargo.toml`, per the house Rust rule against hand-edited versions.
  Confirmed `3.1.0` is genuinely latest with `cargo search rmcp` first.
- `tests/wire.rs` is its own integration binary with exactly ONE test, mirroring
  `tests/stdout.rs`. Both redirect the process's real fd 1; the libtest harness writes
  each test's `... ok` progress line to that same fd, so a sibling test finishing
  inside the capture window would corrupt the assertion. `tests/stdout.rs` documents
  this and the new file inherits the constraint.
- Frames are asserted as whole `serde_json::Value` objects rather than by field, so an
  ADDED field fails too. `resultType: "complete"` (what rmcp emits at protocol
  >= 2026-07-28) is precisely the regression class this must catch, and a
  field-by-field assertion would silently pass it.
- Server identity in the test is pinned to literals (`wire-test` / `0.0.0`), NOT
  `env!("CARGO_PKG_VERSION")`. A wire test must not need re-baselining every time the
  crate version moves. This is also the defect that broke the doc's md5 criterion.
- `serve_with` (the transport-generic core) is private, so the integration test drives
  the public `serve` over real fd 0/1. That is the better test anyway: it exercises
  the exact production path a host runs.

### Deviations
- **The doc's Phase 1 success criterion "the spike handshake md5 still equals
  `40e80ec22e610409bd519fd3df9b0089`" was AMENDED in the doc rather than satisfied.**
  It was broken two independent ways, neither fixable by any implementation choice:
  - It hashes an order-nondeterministic stream. rmcp dispatches each request as its
    own task, so the `tools/list` (id 2) and `tools/call` (id 3) responses race.
    Measured over 20 runs: `40e80ec22e610409bd519fd3df9b0089` (ids 1,2,3) 9 times,
    `076cf1054f725872a7e16c5574760977` (ids 1,3,2) 11 times. Identical content, only
    line order differs, so the criterion fails ~half the time for correct code.
    Piping through `sort` first is stable 10/10 and reproduces the recorded value
    exactly, so the recorded digest was right and the command was missing a `sort`.
  - The digest is crate-version sensitive and this phase's own `bump -m` changes it.
    `examples/spike.rs` reports `serverInfo.version` from `env!("CARGO_PKG_VERSION")`.
    Pre-bump (0.2.0) sorted digest `40e80ec22e610409bd519fd3df9b0089`; post-bump
    (0.3.0) `e29861f4e2950d5e7b73eada63ca9303`, stable 5/5, differing in exactly the
    one field `serverInfo: {"name":"spike","version":"0.3.0"}`.
  - Both amendments are recorded in the design doc next to the criteria they touch,
    with the command output. A3's conclusion is UNCHANGED: frame content really is
    identical across 2.2 and 3.1. Only the digest mechanism was unsound.
- `bump --no-tag -m` bumped `Cargo.toml` to 0.3.0 but its own `git add` failed with
  `.claude/hooks: can only add regular files, symbolic links or git-directories`. That
  is the known Claude Code sandbox phantom-file bug (bwrap deny-mounts leaking into
  the working dir), not a repo or `bump` defect. The version edit `bump` produced was
  correct and complete, so it was committed as `Bump version to v0.3.0`, matching the
  convention of the existing `8b58bcc "Bump version to v0.1.3"`. No version was
  hand-authored; the value came from `bump`.

### Tradeoffs
- Whole-frame `Value` equality vs. exact byte comparison: byte comparison would also
  catch key-order changes, but JSON key order here is a serde struct-field artifact
  with no protocol meaning, so it would generate false failures. `Value` equality
  still catches added, removed, renamed and changed fields, which is the actual
  guarantee. Chose `Value` equality.
- Matching responses by JSON-RPC `id` vs. by arrival position: position is simpler and
  was the first draft. It FAILED intermittently in `otto ci` (passed one run, failed
  the next, with `frames[1]` holding the id-3 response). Out-of-order responses are
  legal JSON-RPC and rmcp genuinely produces them, so `id` matching is correct
  semantics rather than a workaround. 25/25 clean after the change, and the flake was
  root-caused rather than retried.
- Keeping `examples/spike.rs` at all: its header claimed rmcp 2.1.0, which the bump
  made false. Corrected in place, with a note that the permanent wire guarantee lives
  in `tests/wire.rs` and not in an example its own header calls throwaway.

### Open questions
- None blocking. One observation for whoever ships Phases 2-5: the doc's per-repo gate
  table lists `marquee` in the ship-order diagram but omits it from the release-flow
  table (the table covers `mcp-io-rs`, `slack-cli`, `clyde`, `persona-cli`). Confirm
  marquee's gates live with `bump --gates` before its release step rather than
  inferring from the table.

### Evidence
- `rg -c '^rmcp = \{ version = "3.1"' Cargo.toml` returns `2`. A1 MET for this repo.
- `Cargo.lock` resolves exactly one rmcp, at `3.1.0`.
- `otto ci` exit 0, `All CI checks passed!`: 45 unit + 1 `stdout` + 1 `wire` + 1
  doctest, 0 failed.
- Source diff outside the new test: EMPTY of logic. Only `Cargo.toml`, `Cargo.lock`,
  the `examples/spike.rs` header comment, and README prose changed. The doc's
  "zero source changes" claim held.
- The wire test was broken twice to prove it bites: once on the `tools/call` text
  (`pong` -> `pang`) and once, after the `id`-matching rewrite, on the `tools/list`
  input schema. Both failed with the offending field named in the diff; both passed
  again on restore.

## Phase 1 addendum: A4's grep was fragile (found while walking criteria)

### Design decisions
- None beyond the amendment below.

### Deviations
- **A4 amended a second time, to anchor its pattern on `^mcp-io = `.** Walking the
  criteria after the phase commit showed each consumer manifest carries several
  git-tag deps, not just `mcp-io`. Measured: slack-cli `v0.1.4` / `v0.5.0` / `v0.3.3`,
  persona-cli `v0.1.2` / `v0.6.0` / `v0.3.3`, marquee/cli `v0.1.2` / `v0.5.0` /
  `v0.3.3`, clyde `v0.1.2` / `v0.3.3`. A4's bare `rg -c 'tag = "<phase-1-tag>"'` is
  safe for the actual Phase 1 tag (`v0.3.0`, which collides with nothing), so this did
  NOT block anything. But `v0.3.3` is three patch releases away and already sits in
  three of the four manifests; at that point the unanchored grep returns 2 and the
  criterion reads as satisfied by an unrelated dependency. Anchored rather than left
  as a trap for whoever runs Phases 2-5.

### Tradeoffs
- Could have left it alone since it is not wrong today. Rejected: the criterion is
  about to be run four more times by phases that have not started, and the failure
  mode is a SILENT false pass, which is the worst kind.

### Open questions
- None.

## Phase 1 addendum 2: review-panel implementation audit (2026-08-04)

Panel run in Mode 2 (Implementation Audit) scoped to Phases 0-1, overriding the
`Status: Approved` auto-detection. Both reviewers returned rc=0.

### Verdicts on the work already done
- **Implementation audited CLEAN.** Zero undisclosed deviations, three-way agreement.
  `git diff origin/main..HEAD -- src/` empty, so the doc's "zero source changes" claim
  held. All Phase 1 bullets verified against the code.
- **Both prior amendments independently confirmed as genuine DOC DEFECTS**, not
  implementation dodges. One reviewer reproduced the md5 nondeterminism from a fresh
  build (20 unsorted runs split across two digests; 20/20 stable once sorted) and
  confirmed via spec that JSON-RPC correlates by `id` and guarantees no response
  ordering, so `| sort` fixes determinism rather than masking a required guarantee.
  Noted for the record: the md5 amendment turned a FAILING criterion into a PASSING
  one, which is the risky direction; it stands only because the defects reproduce.
- **`tests/wire.rs` proven to bite empirically**, beyond my own break-test: a reviewer
  flipped `PROTOCOL_VERSION` to `2026-07-28`, rmcp 3.1 emitted
  `resultType: "complete"`, and the test failed naming that field, then passed again on
  restore. So both halves are confirmed: rmcp really emits it, and whole-`Value`
  equality really catches it.
- **`bump` commit confirmed equivalent** to what `bump` would have produced (same
  narrow two-file shape as `8b58bcc`), and the repo state supports the post-merge
  `bump --tag-only`.

### Deviations: three doc defects the panel caught that MY criteria walk missed
All three are in phases NOT yet executed, so none is an implementation deviation. All
three would have caused a phase to false-fail or run with no gate. Fixed in the doc:
- **Phase 3 (clyde) shipped with NO success criteria whatsoever.** Root cause is
  mechanical: `542da55` inserted the persona-cli phase and shifted every following
  criteria block up by one, so Phase 3 lost its block and Phase 5 gained a second. The
  header and block COUNTS still matched (6 and 6), which is exactly why it survived
  five review passes plus a review panel. Verified by mapping blocks to headers rather
  than counting them. Worst possible phase to leave ungated: it is the only one where
  two crates must move in a single commit or the workspace links two rmcp majors.
- **Phase 5 carried a second, contradictory criteria block** demanding exactly one rmcp
  at 3.1.0 in marquee's lock, contradicting its own body and A2, both of which say two
  majors is correct there. Orphan from `b530c89`, never deleted when `7fc0a16` inserted
  the correct block. Deleted, and the surviving criterion now asserts the two-major end
  state POSITIVELY (`1.8.0` and `3.1.0`) instead of leaving it implied.
- **Phase 4's own success criterion still carried the exact unanchored grep that A4 was
  amended to fix.** I anchored the acceptance criterion and left the per-phase criterion
  a phase-runner would actually execute. Same trap, same file, one amendment short.
  Anchored.

### Also folded in (low)
- `marquee` row ADDED to the release-flow table. Measured `bump --gates` in all five
  repos: `mcp-io-rs`/`slack-cli`/`clyde` gated, `persona-cli`/`marquee` ungated. The
  table had four rows against a five-repo ship order, so Phase 5's release flow was
  unstated. This closes the open question raised in the Phase 1 notes above.
- Dependencies section amended: it listed only the original three repos and omitted
  `persona-cli` and `marquee/cli`, which Phases 4-5 change.
- Phase 0's "re-register the INSTALLED clyde" bullet marked CONDITIONAL, with the
  mechanism cited (`src/register/claude.rs:156-172`, `CLAUDE_CONFIG_DIR` resolution).
  As written it only cleans up a mess that following it creates.
- Testing Strategy now NAMES the wire test's two coverage limits rather than implying
  them: it guards only the `< 2026-07-28` frame shape, and error frames are uncovered.

### Tradeoffs
- Put the three must-fixes INLINE next to their phases rather than in Open Questions.
  Open Questions is for undecided things; these are decided defects with fixes, and the
  house rule is to never build with open questions. A phase-runner reads its own phase,
  not a list at the bottom.
- Declined the panel's suggestion to parse the manifests with a TOML parser instead of
  an anchored `rg`. Both reviewers raised it and both demoted it. The anchored form
  fails CLOSED on the multiline `[dependencies.mcp-io]` shape, and every manifest in
  play uses the house single-line inline-table form. Speculative robustness for a
  one-line acceptance check.

### Open questions
- None. The marquee gate question from the Phase 1 notes is now closed by measurement.

## Scope widened 2026-08-04: every rmcp use goes to 3.1

Scott: "I WANT THE FUCKING USES TO ALL GO TO rmcp 3.1", then "amend the doc to do it
all. no partials, no half-assing it." Doc amended to match. Phases 0-1 are unaffected and
stay shipped.

### Design decisions
- The doc's scope is now defined by the DEPENDENCY (rmcp) rather than by the wrapper we
  own (`mcp-io`). Added A7, an org-wide `gh search code --owner tatari-tv 'rmcp
  filename:Cargo.toml'` assertion, so a future target cannot hide the way these two did.
  A7 is a search, deliberately not a list of repos.
- Phases 5 and 6 merged into ONE commit in ONE worktree: same repo, one workspace lock.
  Kept as two numbers because Phase 5 is tag-gated and Phase 6 is not.
- Phase 7 tagged **opus** rather than sonnet, unlike every other phase. It is the only
  phase with real migration work (12 errors, four of them non-exhaustive-struct rewrites
  that are not mechanical renames).

### Deviations
- **`marquee/mcp` moved from Non-Goal to in-scope.** The original exclusion is preserved
  in the doc with its reasoning, struck through rather than deleted. Worth recording WHY
  it fell, because the two halves failed differently: the "hard fact" (no `mcp-io`
  dependency) was TRUE but only ever proved the tag could not reach the crate, which is
  about ordering, not scope. The other half was an unmeasured cost estimate ("two-major
  jump", "hand-writes its own ServerHandler") that a probe contradicted at ONE line.
- **`platform-templates` added as Phase 7.** Never excluded; never seen. The doc's
  inventory searched for `mcp-io`, so it was structurally incapable of finding an rmcp
  user that does not depend on `mcp-io`. The doc had even recorded the lesson from its
  earlier `persona-cli` miss ("do not hand-maintain this list") and then repeated it one
  level up by searching for the wrapper instead of the dependency.
- **Phase 5's lock criterion re-amended, second time in one day.** This morning a stray
  contradictory block was deleted and the survivor rewritten to assert TWO rmcp entries
  (1.8.0 + 3.1.0). With `mcp/` now in scope the correct assertion is ONE entry at 3.1.0.
  Both amendments are recorded in place rather than the second quietly reverting the
  first, because the deleted block's ASSERTION was right for today's scope and wrong for
  this morning's: it was orphaned, not merely incorrect.
- **A2 no longer excludes marquee**, since its workspace now resolves a single rmcp.
  `platform-templates` is excluded from A2 for a different reason than the others: it
  ships no `Cargo.lock` at all, because the skeletons are templates rather than built
  artifacts. A7 covers it.
- **A6 marked DISCHARGED** with the real-tag evidence, rather than left pending.
- Rollout Plan corrected: the old "every one is a CLI, so rollout is `cargo install`" is
  false now. `marquee/mcp` is a deployed HTTP service and needs a live probe;
  `platform-templates` neither deploys nor installs and is verified by generating a
  service that compiles.

### Tradeoffs
- Widening this doc vs. giving `platform-templates` its own. I recommended splitting it
  and was overruled; recorded because the reasoning still constrains execution. Phase 7
  shares nothing with Phases 1-6 operationally: no `mcp-io` dep, no tag gate, not a cargo
  repo, no version to bump, a yarn golden harness instead of `otto ci`. Those differences
  are now called out explicitly in the phase so a runner does not try `bump` or `otto`
  there. One doc, but the phase stands alone.
- Phase 6's criteria assert the literal `with_legacy_session_mode(false)` rather than just
  compilation. Slightly brittle to reformatting, and worth it: 3.1 flipped the default to
  `true`, so a dropped argument silently enables sessions on a gateway-fronted server. A
  compile-only check passes on that bug.

### Open questions
- None.

### Evidence
- `marquee` whole workspace on 3.1: `cargo check --workspace --all-targets` clean, clippy
  clean on `marquee-mcp` + `marquee-cli`, **170 tests pass**. One source line changed. The
  probe pointed `cli` at the REAL `tag = "v0.3.0"`, so it re-proved A6 independently.
- `platform-templates` axum golden on 3.1: **12 errors, all in `src/mcp.rs`**. Categories
  and fixes recorded in the doc's probe section.
- Gates measured with `bump --gates`, all six repos: `mcp-io-rs`, `slack-cli`, `clyde`,
  `platform-templates` GATED; `persona-cli`, `marquee` UNGATED.
- `platform-templates` shape confirmed: root is `package.json`/yarn with no cargo version,
  goldens regenerate via `node tests/harness/run.js --update`.

## CodeRabbit review of the scope amendment (PR #14, 2026-08-04)

Three threads. Two folded in, one pushed back with evidence.

### Deviations
- **FIXED, CodeRabbit was right: "~13 majors" was wrong.** I wrote that
  `platform-templates` sits "~13 majors behind" in three places, conflating the MINOR
  version in `0.13` with a count of majors. `0.13` -> `3.1` crosses three majors
  (`0.x` -> `1.x` -> `2.x` -> `3.x`). Corrected in all three places with the path spelled
  out. The security argument is unaffected in substance (the scaffolder is still the
  staleness that regenerates itself) but the number was simply false, and a doc that
  overstates its own evidence is the thing this project keeps getting bitten by.
- **FIXED, CodeRabbit was right: Phase 7's build criterion covered one golden, not three.**
  The criterion said `cargo check` "inside at least one regenerated golden" while the phase
  scope is actix + axum + rocket. The three share `skeletons/mcp/src/mcp.rs` but NOT their
  web framework, so they differ in how the MCP service is mounted and a fix proven on axum
  leaves two thirds unproven. Now requires all three. Also recorded WHY the probe measured
  only axum: at probe time the question was "how big is this migration", which is a
  different question from "is it done".

### Pushed back
- **REJECTED: "the count is 13 manifests, not 12; add `clyde/clyde/Cargo.toml` to A7."**
  `clyde/clyde/Cargo.toml` does NOT declare rmcp. Verified: it declares exactly one
  relevant dependency, `mcp-io = { git = ..., tag = "v0.1.2", version = "0.1.2" }`, and
  receives rmcp TRANSITIVELY through `mcp-io`. Only `clyde/sessions/Cargo.toml` declares
  rmcp directly (`2.1.0`). The authoritative
  `gh search code --owner tatari-tv 'rmcp filename:Cargo.toml'` returns `clyde/sessions`
  and not `clyde/clyde`, which is the whole point of A7 being a search over rmcp rather
  than a hand-built list. 12 is correct.
  Worth noting the distinction the finding blurred, since Phase 3 depends on it: clyde has
  TWO crates that must move together, but for different reasons. `clyde/sessions` moves
  because it pins rmcp; `clyde/clyde` moves because it pins the `mcp-io` tag. That is
  already documented in Phase 3 and is exactly why that phase is one commit.

### Open questions
- None.
